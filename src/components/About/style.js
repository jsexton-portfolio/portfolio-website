import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles((theme) => ({
  root: {
    padding: 30,
    textAlign: 'center'
  },
  sectionContainer: {
    marginTop: 100
  },
  textArea: {
    padding: 10,
    marginTop: 100
  }
}))
