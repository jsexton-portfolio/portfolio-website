import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(theme => ({
  textContainer: {
    marginTop: 50,
    textAlign: 'center'
  },
  formItem: {
    marginTop: 40
  }
}))
