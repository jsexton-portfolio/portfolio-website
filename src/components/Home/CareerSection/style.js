import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(theme => ({
  headerText: {
    fontSize: 35
  },
  paragraphText: {
    fontSize: 25
  },
  projects: {
    paddingTop: 75
  }
}))
